# Offline toolchain for the C++ model — NOT needed to build or run the C++ engine
# itself (that is pure CMake/C++). These provision the C++ inputs + test oracles:
#   provision.py      HF download + pack weights (model.mmfree) + pack tokenizer
#                     (tokenizer.mmtok) — one command does all three
#   dump_golden.py    PyTorch oracle -> golden/*.npy  (torch, numpy)  [for ctest]
#   reference.py      standalone PyTorch reference (torch, safetensors)
#   compare_prompts.py / run_prompt.py / check_tokenizer.py  (validation helpers)
#
# The original PyTorch/Triton model (mmfreelm + triton-cpu) was removed once the
# C++ implementation was verified to match it — so triton/transformers-as-model
# are gone; transformers remains only for AutoTokenizer in the helper scripts.
torch
safetensors
numpy
transformers
huggingface_hub
